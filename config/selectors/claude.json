{
  "appKey": "claude",
  "_comment": "Verified 2026-06-30 from claude.ai DOM inspection. Input is ProseMirror (tiptap) with data-testid='chat-input'. Send button is button[aria-label='Send message'] (only visible while text is typed). Response bubble is div.font-claude-response. Stop button selector pending — only visible during generation.",
  "selectors": {
    "input": [
      "div[data-testid='chat-input']",
      "div[aria-label='Write your prompt to Claude']",
      "div.tiptap.ProseMirror[contenteditable='true']"
    ],
    "send": [
      "button[aria-label='Send message']",
      "button[aria-label='Send']",
      "button[type='submit']"
    ],
    "response": [
      "div.font-claude-response",
      "div.standard-markdown"
    ],
    "completion": [
      "button[aria-label='Stop']",
      "button[aria-label='Stop generating']"
    ],
    "blocked": [],
    "loginError": [
      "a[href*='/login']"
    ]
  }
}
