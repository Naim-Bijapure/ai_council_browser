{
  "appKey": "chatgpt",
  "_comment": "Verified 2026-06-30 from chatgpt.com DOM inspection. Input is ProseMirror div#prompt-textarea (contenteditable). Hidden fallback textarea excluded — would match invisible element. Send button has data-testid='send-button' when text is typed (aria-label='Start Voice' when empty). Response bubble is div.markdown.prose. Stop button only appears during generation.",
  "selectors": {
    "input": [
      "div#prompt-textarea",
      "div[contenteditable='true'][role='textbox']",
      "div[contenteditable='true'][aria-label='Chat with ChatGPT']"
    ],
    "send": [
      "button[data-testid='send-button']",
      "button[aria-label='Send prompt']"
    ],
    "response": [
      "div[data-message-author-role='assistant']",
      "div.markdown.prose"
    ],
    "completion": [
      "button[data-testid='stop-button']",
      "button[aria-label='Stop generating']"
    ],
    "blocked": [
      "div[data-testid='rate-limit-message']",
      "div.captcha-container",
      "iframe[src*='captcha']"
    ],
    "loginError": [
      "a[href*='/auth/login']",
      "a[href*='/auth/signup']"
    ]
  }
}
